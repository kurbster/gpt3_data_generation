defaults:
  - outputs
  - experiment_defaults
  - preprocessing_config: boolq
  - _self_
hydra:
  env:
    save_name: "${dataset_name}_${model_name}"

model_name: "facebook/bart-base"
dataset_name: "boolq"

train_files:
  - "datasets/${dataset_name}/gpt3_generated_500sample.json"

output_dir: "models/${dataset_name}/${model_name}"

num_labels: 2
max_train_samples: 468
